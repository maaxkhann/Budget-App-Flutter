import 'package:flutter/material.dart';

class ExpenseViewWeb extends StatelessWidget {
  const ExpenseViewWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
